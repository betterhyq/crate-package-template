{
  "scripts": {
    "test": "cargo test -- --show-output",
    "build": "cargo build",
    "build:release": "cargo build --release",
    "format": "cargo fmt"
  },
  "packageManager": "pnpm@10.28.0"
}
